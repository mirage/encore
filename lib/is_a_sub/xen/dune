(library
 (name        is_a_sub_xen)
 (public_name encore.is_a_sub.xen)
 (optional)
 (libraries   mirage-xen-posix)
 (c_names     is_a_sub)
 (c_flags     (:include cflags.sexp)))

(rule (copy# ../is_a_sub.c is_a_sub.c))

(rule (with-stdout-to cflags.sexp (run ./cflags.sh)))
